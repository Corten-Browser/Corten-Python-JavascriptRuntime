contract:
  name: es2024_string_methods
  version: 0.3.5

methods:
  - isWellFormed(): Check for unpaired surrogates
  - toWellFormed(): Replace unpaired surrogates with U+FFFD

test_requirements:
  unit_tests: ≥13
  coverage: ≥95%
