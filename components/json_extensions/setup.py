"""Setup for json_extensions component"""

from setuptools import setup, find_packages

setup(
    name="json_extensions",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.11",
)
